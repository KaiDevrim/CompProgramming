namespace LeetCode.Solutions.Solutions;

public class PalindromeNumber
{
    public bool IsPalindrome(int x)
    {
        Console.WriteLine(x.ToString().Reverse().ToString());
        return true;
    }
}